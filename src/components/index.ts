import { motion } from 'framer-motion';

export const Section = motion.section;

export const sectionClasses = "w-full max-w-[1200px] mx-auto my-32 p-4";

export const mobile = "max-w-[768px]";

// import { motion } from 'framer-motion';
// import styled from 'styled-components';

// export const Section = styled(motion.section)`
//   width: 100%;
//   max-width: 1200px;
//   margin: 128px auto;
//   padding: 16px;
// `;

// export const mobile = '@media (max-width: 768px)';